# This helps you handle all columns correctly
insert_cols = df_target.columns

# Build a mapping from target col → source col (with _s if needed)
source_cols = []
for col in insert_cols:
    if col + '_s' in df_insert.columns:
        source_cols.append(col + '_s')
    elif col in df_insert.columns:
        source_cols.append(col)
    else:
        # Fill missing columns with NaNs
        df_insert[col + '_missing'] = pd.NA
        source_cols.append(col + '_missing')

# Create df_insert_clean with aligned columns
df_insert_clean = df_insert[source_cols].copy()
df_insert_clean.columns = insert_cols  # Rename to match df_target structure