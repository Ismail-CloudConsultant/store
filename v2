import zipfile
import os

def unzip_file(zip_path, extract_to):
    """Extract a zip file to a folder."""
    with zipfile.ZipFile(zip_path, 'r') as zip_ref:
        zip_ref.extractall(extract_to)

# Example usage
unzip_file("my_folder.zip", "unzipped_folder")